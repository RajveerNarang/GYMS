{
  "builds": [
    {
      "src": "inject-env.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/index.html" }],
  "scripts": {
    "vercel-build": "node inject-env.js"
  }
}
